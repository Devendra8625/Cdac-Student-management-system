services:
  # 0. MySQL Database
  mysql-db:
    image: mysql:8.0
    container_name: mysql-db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASS:-root}
      MYSQL_DATABASE: ${DB_NAME:-cdac_sms_db}
      MYSQL_USER: ${DB_USER:-cdacuser}
      MYSQL_PASSWORD: ${DB_PASS:-cdacpass}
    ports:
      - "3307:3306"
    volumes:
      - mysql-data:/var/lib/mysql

  # 1. Eureka Service Registry
  service-registry:
    build:
      context: ./backend
      dockerfile: service-registry/Dockerfile
    container_name: service-registry
    restart: always
    ports:
      - "8761:8761"

  # 2. API Gateway
  api-gateway:
    build:
      context: ./backend
      dockerfile: api-gateway/Dockerfile
    container_name: api-gateway
    restart: always
    ports:
      - "8080:8080"
    environment:
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://service-registry:8761/eureka/
    depends_on:
      - service-registry

  # 3. Auth Microservice
  auth-service:
    build:
      context: ./backend
      dockerfile: auth-service/Dockerfile
    container_name: auth-service
    restart: always
    ports:
      - "8081:8081"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://${DB_HOST:-mysql-db}:3306/${DB_NAME:-cdac_sms_db}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
      - SPRING_DATASOURCE_USERNAME=${DB_USER:-cdacuser}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASS:-cdacpass}
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://service-registry:8761/eureka/
    depends_on:
      - mysql-db
      - service-registry

  # 4. Student Microservice
  student-service:
    build:
      context: ./backend
      dockerfile: student-service/Dockerfile
    container_name: student-service
    restart: always
    ports:
      - "8082:8082"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://${DB_HOST:-mysql-db}:3306/${DB_NAME:-cdac_sms_db}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
      - SPRING_DATASOURCE_USERNAME=${DB_USER:-cdacuser}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASS:-cdacpass}
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://service-registry:8761/eureka/
    depends_on:
      - mysql-db
      - service-registry

  # 5. Course Microservice
  course-service:
    build:
      context: ./backend
      dockerfile: course-service/Dockerfile
    container_name: course-service
    restart: always
    ports:
      - "8083:8083"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://${DB_HOST:-mysql-db}:3306/${DB_NAME:-cdac_sms_db}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
      - SPRING_DATASOURCE_USERNAME=${DB_USER:-cdacuser}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASS:-cdacpass}
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://service-registry:8761/eureka/
    depends_on:
      - mysql-db
      - service-registry

  # 6. Marks Microservice
  marks-service:
    build:
      context: ./backend
      dockerfile: marks-service/Dockerfile
    container_name: marks-service
    restart: always
    ports:
      - "8084:8084"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://${DB_HOST:-mysql-db}:3306/${DB_NAME:-cdac_sms_db}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
      - SPRING_DATASOURCE_USERNAME=${DB_USER:-cdacuser}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASS:-cdacpass}
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://service-registry:8761/eureka/
    depends_on:
      - mysql-db
      - service-registry

  # 7. Attendance Microservice
  attendance-service:
    build:
      context: ./backend
      dockerfile: attendance-service/Dockerfile
    container_name: attendance-service
    restart: always
    ports:
      - "8085:8085"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://${DB_HOST:-mysql-db}:3306/${DB_NAME:-cdac_sms_db}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
      - SPRING_DATASOURCE_USERNAME=${DB_USER:-cdacuser}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASS:-cdacpass}
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://service-registry:8761/eureka/
    depends_on:
      - mysql-db
      - service-registry

  # 8. Assignment Microservice
  assignment-service:
    build:
      context: ./backend
      dockerfile: assignment-service/Dockerfile
    container_name: assignment-service
    restart: always
    ports:
      - "8086:8086"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://${DB_HOST:-mysql-db}:3306/${DB_NAME:-cdac_sms_db}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
      - SPRING_DATASOURCE_USERNAME=${DB_USER:-cdacuser}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASS:-cdacpass}
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://service-registry:8761/eureka/
    depends_on:
      - mysql-db
      - service-registry

  # 9. Notice Microservice
  notice-service:
    build:
      context: ./backend
      dockerfile: notice-service/Dockerfile
    container_name: notice-service
    restart: always
    ports:
      - "8087:8087"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://${DB_HOST:-mysql-db}:3306/${DB_NAME:-cdac_sms_db}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
      - SPRING_DATASOURCE_USERNAME=${DB_USER:-cdacuser}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASS:-cdacpass}
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://service-registry:8761/eureka/
    depends_on:
      - mysql-db
      - service-registry

  # 10. Placement Microservice
  placement-service:
    build:
      context: ./backend
      dockerfile: placement-service/Dockerfile
    container_name: placement-service
    restart: always
    ports:
      - "8088:8088"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://${DB_HOST:-mysql-db}:3306/${DB_NAME:-cdac_sms_db}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
      - SPRING_DATASOURCE_USERNAME=${DB_USER:-cdacuser}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASS:-cdacpass}
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://service-registry:8761/eureka/
    depends_on:
      - mysql-db
      - service-registry

  # 11. Feedback Microservice
  feedback-service:
    build:
      context: ./backend
      dockerfile: feedback-service/Dockerfile
    container_name: feedback-service
    restart: always
    ports:
      - "8089:8089"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://${DB_HOST:-mysql-db}:3306/${DB_NAME:-cdac_sms_db}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
      - SPRING_DATASOURCE_USERNAME=${DB_USER:-cdacuser}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASS:-cdacpass}
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://service-registry:8761/eureka/
    depends_on:
      - mysql-db
      - service-registry

  # 12. React Frontend Application
  react-frontend:
    build: ./Cdac-Student-management-system-main
    container_name: react-frontend
    restart: always
    ports:
      - "3000:80"
    depends_on:
      - api-gateway

volumes:
  mysql-data:

networks:
  default:
    name: cdac-microservices-net
